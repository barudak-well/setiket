name: 🔨🧪 Build & Test

permissions: write-all

on:
  pull_request:
    branches:
      - "main"

jobs:
  clientTest:
    name: 🧪 Client Test
    uses: ./.github/workflows/__client_test.yml
    secrets: inherit

  serverTest:
    name: 🧪 Server Test
    uses: ./.github/workflows/__server_test.yml
    secrets: inherit
