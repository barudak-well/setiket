import 'package:flutter/material.dart';
import 'package:setiket/src/constants/themes/palette.dart';

/// [INFO]
/// Constant for colors to be used in the app with following the design system
class ColorApp {
  static const Color primary = Palette.color;
  static const Color white = Palette.colorWhite;
  static const Color gray = Palette.colorGray;
  static const Color red = Palette.colorRed;
  static const Color green = Color(0xFF29D697);
  static const Color orange = Color(0xFFF59762);
  static const Color blue = Color(0xFF46CDFB);
  static const Color black = Color(0xFF120D26);
}
